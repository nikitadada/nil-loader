# nil-loader

**gRPC Load Testing Tool** с real-time дашбордом и автоматическим определением точки деградации.

---

## Презентация

### Слайд 1 — Титульный

**nil-loader**
gRPC Load Testing Tool

- Нагрузочное тестирование gRPC-сервисов
- Real-time дашборд с графиками
- Автоматическое определение точки деградации
- Динамическое управление нагрузкой

> Скриншот: главная страница UI с пустым дашбордом и боковой панелью конфигурации.

---

### Слайд 2 — Зачем нужно нагрузочное тестирование

**Проблема:** Сервис работает на 10 пользователях, но что будет на 10 000?

Нагрузочное тестирование отвечает на вопросы:
- Какой **максимальный RPS** выдерживает сервис?
- При какой нагрузке начинается **деградация**?
- Какой RPS **безопасен** для продакшена?
- Где **узкое горло**: CPU, память, сеть, база?

Существующие инструменты (ghz, k6, Locust) не всегда поддерживают gRPC «из коробки», требуют кодогенерации или не дают real-time визуализацию.

> Картинка: схема «клиент → load tester → gRPC сервис», стрелки с RPS.

---

### Слайд 3 — Эффект от использования nil-loader

**Какой эффект даёт MVP:**

- **Сокращение времени** подготовки полноценного нагрузочного теста с gRPC:
  - до nil-loader: 2–3 часа на написание скриптов, сбор метрик и ручной разбор результатов
  - с nil-loader: **10–15 минут** от ввода адреса сервиса до отчёта с точкой деградации
- **Явное определение безопасного RPS**:
  - один прогон Ramping‑теста → автоматически считаем **точку деградации** и **рекомендуемый RPS (≈80%)**
  - уменьшаем риск инцидентов в проде, когда нагрузка неожиданно вырастает
- **Прозрачность для команды**:
  - единый real-time дашборд вместо разрозненных логов и дашбордов
  - упрощённая коммуникация между разработчиками, SRE и продуктом: «сервис стабилен до X RPS, safe = Y RPS»

**Количество / воспроизводимость:**

- для тестового example-сервиса:
  - nil-loader стабильно находит точку деградации в районе **800–900 RPS**
  - рекомендуемый безопасный RPS — **~650–700 RPS** (80% от точки деградации)
- сценарий можно повторить локально на любой машине по инструкции из README и получить сопоставимые результаты.

---

### Слайд 4 — Базовые термины

| Термин | Описание |
|---|---|
| **RPS** (Requests Per Second) | Количество запросов в секунду — основная метрика нагрузки |
| **Latency** | Время ответа на один запрос (в миллисекундах) |
| **p50 / p95 / p99** | Перцентили латентности. p99 = 99% запросов быстрее этого значения |
| **Throughput** | Реальная пропускная способность (фактический RPS) |
| **Concurrency** | Количество параллельных соединений / горутин |
| **Error Rate** | Процент неуспешных запросов |
| **Точка деградации** | RPS, при котором латентность или ошибки начинают расти нелинейно |
| **HDR Histogram** | Алгоритм для точного расчёта перцентилей без большого потребления памяти |

> Картинка: график с тремя линиями p50/p95/p99, показывающий «расслоение» при нагрузке.

---

### Слайд 4 — Архитектура nil-loader

```
┌─────────────┐     HTTP/WS      ┌─────────────────┐     gRPC      ┌──────────────┐
│   Browser   │ ◄─────────────►  │   nil-loader    │ ────────────► │  Target      │
│   (UI)      │   port 8080      │   (Go backend)  │   dynamic     │  Service     │
└─────────────┘                  └─────────────────┘               └──────────────┘
                                        │
                              ┌─────────┼─────────┐
                              │         │         │
                          Engine    Telemetry   gRPC Client
                        (rate limiter,  (HDR hist,   (reflection,
                        worker pool)  degradation)  dynamic msg)
```

**Монолит**: бекенд + статика в одном бинарнике. Данные в памяти.

> Скриншот: нет (схема в самом слайде).

---

### Слайд 5 — Фишка 1: Dynamic gRPC без кодогенерации

**Проблема:** Обычные gRPC-клиенты требуют `protoc` + генерацию Go-кода под каждый сервис.

**Решение в nil-loader:**
- **gRPC Reflection** — указываем адрес сервиса → автоматически получаем список сервисов и методов
- **Загрузка .proto на лету** — вставляем proto-определение прямо в UI
- **Dynamic Message** — формирование запросов без генерации кода (`jhump/protoreflect`)

Пользователь просто вводит `localhost:50051`, нажимает **Discover** и видит все доступные методы.

> Скриншот: боковая панель с результатом Discover via Reflection — список сервисов и методов.

---

### Слайд 6 — Фишка 2: Динамическое управление нагрузкой

**Два режима:**

| Режим | Описание |
|---|---|
| **Constant** | Фиксированный RPS на всё время теста |
| **Ramping** | Плавный рост от Start RPS до End RPS за указанное время |

**Worker Pool** с ограничением concurrency — не «кладём» машину-тестировщик.

Ramping позволяет найти точку отказа: нагрузка растёт линейно, и мы видим момент, когда сервис перестаёт справляться.

> Скриншот: график Throughput (RPS) — Target vs Actual, где видно расхождение при высокой нагрузке.

---

### Слайд 7 — Фишка 3: Генератор данных

**Проблема:** Тестирование одинаковым payload → кэширование → нереалистичные результаты.

**Решение — шаблоны с Faker-токенами:**

```json
{
  "user_id": "{{faker.uuid}}",
  "name": "{{faker.name}}",
  "email": "{{faker.email}}",
  "phone": "{{faker.phone}}"
}
```

Каждый запрос получает **уникальные данные**.

Доступные токены: `uuid`, `email`, `name`, `firstName`, `lastName`, `phone`, `username`, `sentence`, `timestamp`, `int`, `bool`.

**CSV-источник:** загрузка данных из CSV для параметризации запросов (`{{csv.column}}`).

> Скриншот: поле Payload Template в UI с faker-шаблоном.

---

### Слайд 8 — Фишка 4: Real-time дашборд

Четыре виджета обновляются каждую секунду через WebSocket:

| Виджет | Что показывает |
|---|---|
| **Latency Chart** | Три линии: p50, p95, p99. Видно «расслоение» при деградации |
| **Throughput** | Target RPS vs Actual RPS. Расхождение = сервис «захлебнулся» |
| **Success Rate** | Donut: OK vs ошибки + данные о деградации |
| **Error Breakdown** | Группировка по gRPC-кодам (Unavailable, DeadlineExceeded, Internal) |

Плюс:
- **Error Log** — последние ошибки с кодом, сообщением и таймстемпом
- **Streaming Logs** — терминал (xterm.js) с логами движка в реальном времени

> Скриншот: полный UI во время активного теста — все графики заполнены данными.

---

### Слайд 9 — Фишка 5: Автоматическое определение точки деградации

**Как работает детектор:**

1. Первые 5 секунд — сбор **baseline p99** (нормальная латентность)
2. Каждую секунду проверяем:
   - p99 вырос > **2.5x** от baseline?
   - Интервальный error rate > **15%**?
3. Если да — фиксируем **точку деградации**

**Результат:**

| Метрика | Описание |
|---|---|
| **Degradation at** | RPS, при котором началась деградация |
| **Recommended** | Безопасный RPS = 80% от точки деградации |
| **Max stable** | Максимальный RPS при стабильной работе |

> Скриншот: блок Success Rate & Degradation с красным бейджем «DEGRADATION DETECTED» и тремя метриками.

---

### Слайд 10 — Телеметрия под капотом

**HDR Histogram** (`hdrhistogram-go`):
- Точные перцентили (p50/p95/p99) без хранения каждого значения
- Диапазон 1µs – 30s, 3 значащие цифры
- Интервальная гистограмма сбрасывается каждую секунду

**Error Classifier:**
- Автоматическая группировка по gRPC status codes
- `Unavailable`, `DeadlineExceeded`, `Internal`, `Unknown` и др.
- Кумулятивные и интервальные счётчики

**Snapshot (1 сек):**
```
timestamp | p50 | p95 | p99 | targetRps | actualRps | success | errors | errorBreakdown
```

> Картинка: нет (данные в самом слайде).

---

### Слайд 11 — Демо: тестируем Example Service

**Тестовый gRPC-сервис** (встроен в проект):
- Метод `ProcessUser` — принимает user_id, name, email, phone
- До 800 RPS: ~5% ошибок, латентность 5–50ms
- Выше 800 RPS: ошибки растут экспоненциально, латентность — квадратично
- Имитирует реальную деградацию

**Сценарий демо:**
1. Discover via Reflection → выбрать ProcessUser
2. Вставить faker-шаблон
3. Ramping 100 → 1500 RPS, 60 секунд
4. Наблюдать деградацию в реальном времени
5. Посмотреть точку деградации ~800–900 RPS

> Скриншот: UI после завершения Ramping-теста — графики с видимой деградацией, бейдж с результатами.

---

### Слайд 12 — Стек технологий и обоснование

| Компонент | Технология | Почему |
|---|---|---|
| Backend | Go 1.25 | Высокая производительность, простая конкуррентность (goroutines), статическая бинарная сборка для одного файла |
| gRPC Dynamic Client | `jhump/protoreflect` | Позволяет вызывать gRPC‑методы без генерации кода, напрямую из reflection / .proto |
| gRPC / Protobuf | Официальный стандарт RPC и описания контрактов | Совместимость с существующей gRPC‑экосистемой, воспроизводимость тестов |
| Metrics | `HdrHistogram/hdrhistogram-go` | Точные p50/p95/p99 без хранения всех значений, подходит для high‑load сценариев |
| WebSocket | `gorilla/websocket` | Надёжная реализация WS в Go, удобная интеграция с существующим HTTP‑сервером |
| Frontend Charts | Chart.js 4 | Лёгкая библиотека для интерактивных графиков, достаточно для дашборда MVP |
| Terminal | xterm.js 5 | Де‑факто стандарт для web‑терминалов, дружелюбен к потоковым логам |
| Сборка | `go:embed` (single binary) | Один бинарник без внешних зависимостей, удобно для локального запуска и CI |

**Зависимости:** минимальные, максимально используется стандартная библиотека Go; все выбранные компоненты хорошо поддерживаются и подходят под нагрузочное тестирование.

> Картинка: нет (таблица в самом слайде).

---

### Слайд 13 — Структура проекта

```
nil-loader/
├── cmd/
│   ├── server/main.go          — UI + API сервер
│   └── testservice/main.go     — тестовый gRPC-сервис
├── internal/
│   ├── model/types.go          — доменные модели
│   ├── engine/engine.go        — движок нагрузки
│   ├── grpcclient/             — reflection, proto, dynamic caller
│   ├── payload/generator.go    — faker + CSV + шаблоны
│   ├── telemetry/
│   │   ├── collector.go        — HDR Histogram + метрики
│   │   └── degradation.go      — детектор деградации
│   └── api/
│       ├── handler.go          — REST API
│       └── ws.go               — WebSocket
├── proto/example/              — proto + сгенерированный код
└── web/index.html              — весь UI (embedded)
```

Репозиторий:
- оформлен как обычный Go‑проект с логичной структурой (`cmd`, `internal`, `proto`, `web`)
- содержит README с требованиями, инструкцией по установке и запуску
- включает лицензию (MIT), что позволяет безопасно использовать инструмент в команде и CI/CD.

> Картинка: нет (дерево в самом слайде).

---

### Слайд 14 — Функциональность MVP и ограничения

**Что уже умеет MVP (основные функции):**
- Запуск нагрузочного теста для одного gRPC‑метода (unary) с режимами **Constant** и **Ramping**
- Автоматическое обнаружение сервисов и методов через gRPC Reflection + загрузка своих `.proto`
- Генерация данных через faker‑шаблоны и CSV‑источник
- Real-time дашборд: p50/p95/p99, Target vs Actual RPS, Success Rate, ошибки по gRPC‑кодам
- Автодетектор точки деградации с расчётом рекомендуемого RPS
- Экспорт JSON‑отчёта по результатам теста.

**Ограничения и допущения текущей версии:**
- Тестируется один gRPC‑метод за раз, без сложных сценариев и цепочек запросов
- Не поддерживаются streaming‑методы (client / server / bidi) — только unary
- Данные и метрики хранятся в памяти, история тестов не сохраняется между перезапусками
- Нет UI‑конфигурации для сложных сценариев авторизации, всё задаётся в ручном виде
- Один инстанс nil-loader — один генератор нагрузки (распределённый режим пока в планах).

---

### Слайд 15 — Интеграции и стандарты

**Стандарты и экосистема:**
- Использует стандартный стек **gRPC + Protobuf** — можно тестировать любые сервисы, уже описанные `.proto`
- WebSocket‑интерфейсы (`/ws/*`) и REST‑API (`/api/*`) возвращают данные в формате **JSON**, удобном для других инструментов
- JSON‑отчёт (`/api/report`) легко парсится в CI/CD‑пайплайне для автоматической проверки SLA.

**Интеграции (как минимум на уровне MVP):**
- **CI/CD**: nil-loader можно запускать в пайплайне (GitLab CI / GitHub Actions) как отдельный шаг:
  - поднять тестовый сервис
  - запустить nil-loader головным бинарником
  - выполнить Ramping‑тест и скачать JSON‑отчёт
  - проверить, что достигнутый RPS и перцентили укладываются в заданные SLO
- **Git / GitHub / GitLab**: проект оформлен как Go‑репозиторий и легко встраивается в существующие mono‑ или multi‑repo.

---

### Слайд 16 — Что дальше (Roadmap)

- [ ] Распределённое тестирование (несколько агентов)
- [ ] Сохранение истории тестов и сравнение результатов
- [ ] Поддержка HTTP/REST помимо gRPC
- [ ] Streaming-методы (client/server/bidi streaming)
- [ ] Экспорт в Prometheus / Grafana
- [ ] CI/CD интеграция — тест как шаг в пайплайне с проверкой SLA

> Картинка: нет (список в самом слайде).

---

### Слайд 17 — Итоги

**nil-loader** решает задачу нагрузочного тестирования gRPC-сервисов:

- **Без кодогенерации** — reflection + dynamic messages
- **С умной нагрузкой** — constant / ramping + worker pool
- **С уникальными данными** — faker + CSV шаблоны
- **С точной телеметрией** — HDR Histogram, посекундные snapshot'ы
- **С real-time UI** — графики, логи, ошибки через WebSocket
- **С автодетекцией деградации** — точка отказа, рекомендуемый RPS

Один бинарник. Ноль конфигурации. Открыл браузер — тестируешь.

> Скриншот: финальный вид UI с завершённым тестом и определённой точкой деградации.

---

## Быстрый старт

```bash
# Запуск тестового gRPC-сервиса
go run ./cmd/testservice/

# Запуск nil-loader (в другом терминале)
go run ./cmd/server/

# Открыть в браузере
open http://localhost:8080
```

## API

| Endpoint | Метод | Описание |
|---|---|---|
| `/api/start` | POST | Запустить тест |
| `/api/stop` | POST | Остановить тест |
| `/api/status` | GET | Текущее состояние |
| `/api/reflect` | POST | Discover сервисов через gRPC Reflection |
| `/api/parse-proto` | POST | Парсинг .proto-определения |
| `/api/degradation` | GET | Результат детекции деградации |
| `/api/report` | GET | Скачать JSON-отчёт |
| `/ws/metrics` | WS | Стрим метрик + статус + деградация |
| `/ws/logs` | WS | Стрим логов движка |
| `/ws/errors` | WS | Стрим ошибок |
